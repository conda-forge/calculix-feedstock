diff --git a/ccx_2.23/src/ccx_2.23.c b/ccx_2.23/src/ccx_2.23.c
index f7cd476..96eb252 100644
--- a/ccx_2.23/src/ccx_2.23.c
+++ b/ccx_2.23/src/ccx_2.23.c
@@ -23,6 +23,7 @@
 #include <math.h>
 #include <stdio.h>
 #include <string.h>
+#include <locale.h>
 #include "CalculiX.h"
 
 #ifdef CALCULIX_MPI
@@ -37,6 +38,12 @@ int main(int argc,char *argv[])
 _set_output_format(_TWO_DIGIT_EXPONENT);
 #endif
 
+  /* the C runtime must format numbers with a decimal point, whatever
+     the user's regional settings are: cgx, FreeCAD and every other
+     reader of .frd/.dat/.net files parse them with the "C" locale */
+
+  setlocale(LC_NUMERIC, "C");
+
   FILE *f1;
     
   char *sideload=NULL,*set=NULL,*matname=NULL,*orname=NULL,*amname=NULL,
diff --git a/ccx_2.23/src/ccx_2.23step.c b/ccx_2.23/src/ccx_2.23step.c
index fcf683b..b545fe5 100644
--- a/ccx_2.23/src/ccx_2.23step.c
+++ b/ccx_2.23/src/ccx_2.23step.c
@@ -23,6 +23,7 @@
 #include <math.h>
 #include <stdio.h>
 #include <string.h>
+#include <locale.h>
 #include "CalculiX.h"
 
 #ifdef CALCULIX_MPI
@@ -44,6 +45,12 @@ void CalculiXstep(int argc,char argv[][133],ITG **nelemloadp,double **xloadp,
 _set_output_format(_TWO_DIGIT_EXPONENT);
 #endif
 
+  /* the C runtime must format numbers with a decimal point, whatever
+     the user's regional settings are: cgx, FreeCAD and every other
+     reader of .frd/.dat/.net files parse them with the "C" locale */
+
+  setlocale(LC_NUMERIC, "C");
+
   /* in FORTRAN convention:
 
      nelemload(1,i) (ITG): element for loading i
